# Game Engine I · Week 12 Visual Generation Log

- Date: 2026-08-19
- Mode: OpenAI built-in image generation
- Output format used by the site: WebP, 1536×1024
- Shared visual reference: `assets/week-10-period2-integration-pipeline.webp` and `assets/week-10-period3-asset-kit-mission.webp`
- Direction: dark tactile tabletop, overhead miniature product photography, black acrylic teaching boards, forest materials, cyan read signals, coral write signals, violet connection signals and lime verification lights
- SVG was not used. All three lesson visuals were generated as raster images and converted to WebP for delivery.

The built-in generator originals remain in the account-managed generated image directory. Site copies were converted with `cwebp -q 88`; the managed originals were not deleted.

## Period 01 · Four control surfaces

- Site asset: `assets/week-12-period1-control-surfaces.webp`
- Managed generation ID: `exec-78276800-ac33-4067-ae6c-1fe222b40b65`
- Inspection result: exactly four main modules are visually separated; the cyan read aperture and coral locked write aperture are clear; no readable text appeared.

Exact prompt:

```text
Create a premium editorial course illustration for a university Unity game-engine class, 3:2 horizontal, 1536x1024. Match the reference image's dark tactile tabletop, museum-like overhead product photography, black acrylic plates with metal corner screws, moss, bark, felt swatches, subtle teal and coral accents, crisp cinematic lighting, high material realism.

SUBJECT: "Four control surfaces and a permission boundary." Build exactly FOUR clearly separated physical modules arranged left to right:
1) an AI gateway portal: a small dark arch receiving a glowing prompt token, guarded by a brass key/credential lock;
2) an MCP relay bridge: a teal bridge connecting an external agent token to a Unity-editor-like miniature scene board, with small tool tokens representing console, hierarchy, and components;
3) a deterministic CLI rail: straight machined tracks carrying identical command blocks toward structured output tiles, visually precise and repeatable;
4) a local Pipeline cable: a short coral data cable connecting a terminal plate directly to a local editor board.

Across the center, add a strong transparent permission boundary with one cyan READ aperture open and one coral WRITE aperture locked. Use iconography only: eye, brackets, console lines, hierarchy nodes, wrench, lock, rollback arrow. Include a small baseline snapshot tile before the modules and a verification/diff tile after them. The visual hierarchy must make the four modules and the read-versus-write boundary immediately understandable.

STYLE/COMPOSITION: orthographic top-down workbench, premium miniature diorama, restrained forest-explorer motifs, deep black background, dark teal, cyan, coral, violet, lime indicator lights; generous negative space; no humans; no mascot character; no screen mockup; no readable text, no letters, no words, no logos, no watermark. Keep every icon clean and non-verbal. Exactly four main modules, not more.
```

## Period 02 · Bounded automation

- Site asset: `assets/week-12-period2-bounded-automation.webp`
- Managed generation ID: `exec-3e2d676d-3fec-4285-8cac-845c088ac943`
- Inspection result: the five stations read left to right; the authorization lane permits one axis; a single coral difference and lime rollback are visually clear; no readable text appeared.

Exact prompt:

```text
Create a premium editorial course illustration for a university Unity game-engine class, 3:2 horizontal, 1536x1024. Match the reference image's dark tactile tabletop, overhead museum-quality product photography, black acrylic boards with metal screws, moss and bark props, dark teal/coral/violet/lime palette, crisp cinematic light, highly realistic miniature materials.

SUBJECT: "Bounded automation demonstration: observe, change exactly one value, verify, rollback." Compose a single left-to-right sequence with exactly FIVE major stations connected by restrained arrows:
1) BASELINE SNAPSHOT: a miniature Unity-like forest scene board with a player capsule, a console tray with zero red error stones, and a transform coordinate block at its original position;
2) READ-ONLY SCAN: a cyan optical scanner passes over console, hierarchy tree, and component tokens without touching them;
3) AUTHORIZATION CONTRACT: a transparent locked frame permits exactly one narrow lane toward one transform axis, while every other tool lane is visibly blocked;
4) ONE REVERSIBLE PROBE: the same empty probe cube moves only along the X axis from the first notch to the adjacent notch, marked by exactly one coral change indicator; nothing else changes;
5) VERIFY AND ROLLBACK: a before/after diff tile shows one changed coordinate, then a curved lime rollback arrow returns the probe cube to the original notch; final console tray still has zero red errors and all baseline indicators match.

Include a small deterministic terminal rail below the sequence with repeatable command blocks feeding a structured grid output, and a separate AI tool bridge above, making clear that both must end in the same manual verification. Use icons only: camera/snapshot, eye, console lines, hierarchy nodes, component cube, lock, one-axis arrow, diff diamonds, rollback.

COMPOSITION: precise instructional diorama with an unmistakable five-step rhythm; generous spacing; exactly one coral change marker; final state visually identical to baseline with green confirmation lights. No people, no mascot, no readable text, no letters, no code, no logos, no watermark, no conventional app screenshot.
```

## Period 03 · Automation readiness mission

- Site asset: `assets/week-12-period3-readiness-mission.webp`
- First generation ID: `exec-ea6c4f30-6c1c-4be1-a3f7-744ac1785191`
- Corrected generation ID used by the site: `exec-9f6fe8f1-9d47-4979-a9fc-a6c2b8ffc956`
- Inspection result: exactly eight lime pass lights and three equal route rows are present. The first generation added readable `45–60 S` text to the camera token despite the non-text instruction, so the built-in image editor removed it and replaced it with non-verbal progress ticks and a check icon. The corrected image is used by the site.

Exact generation prompt:

```text
Create a premium editorial mission-board illustration for a university Unity game-engine class, 3:2 horizontal, 1536x1024. Match the reference's dark tactile tabletop, top-down museum product photography, black acrylic mission board with metal corner screws, small forest diorama pieces, moss and bark, teal/coral/violet/lime palette, realistic miniature materials, polished cinematic light.

SUBJECT: "Student automation readiness mission with equal connected and offline routes." In the center, create one large verification board. Across its top edge place exactly EIGHT evenly spaced lime indicator lights, all illuminated, representing eight pass tests. Arrange the evidence on the board as a clean loop:
- a baseline snapshot card with a miniature forest scene and an empty probe cube at the origin;
- a console evidence tile with warning/error counters represented only by colored stones;
- a hierarchy and component inventory tile represented by node and cube icons;
- an authorization contract plate showing one permitted X-axis lane and all other actions locked;
- a before/after comparison with exactly one coral difference marker;
- a rollback tile showing the probe cube returned to its original origin, with matching green baseline markers;
- a redacted evidence camera token and a structured log sheet with abstract lines;
- a final manual verification seal.

At the left side, show exactly THREE route tokens converging into the same board:
A) connected route, represented by an agent-to-MCP bridge plus terminal;
B) CLI-only route, represented by deterministic command rails plus a manual hand-tool token;
C) offline fixture route, represented by a sealed sample-data envelope plus manual inspector token.
All three paths must arrive at the identical eight-test board; none should look inferior.

Add a compact miniature Unity-like forest scene to the right showing the probe cube at the origin and no error alert. Include a camera token for a 45–60 second evidence recording. Make the visual narrative unmistakable: baseline → read-only evidence → one authorized reversible change → rollback → proof.

No people, no hands, no mascot, no readable text, no letters, no numbers, no code, no logos, no watermark, no app screenshot. Iconography only. Exactly eight top indicator lights and exactly three route tokens.
```

Exact correction prompt:

```text
Edit this existing image while preserving the composition, materials, lighting, colors, exactly eight lime indicator lights at the top, exactly three route rows at the left, the central evidence loop, and all other details.

Make only this correction: on the circular camera token at the bottom right, remove the readable "45–60 S" characters completely. Replace those characters with a clean arc of small non-verbal lime progress tick marks and one tiny completion check icon. Do not add any other letters, numbers, words, code, logos, or watermark anywhere. The finished camera token must communicate a short evidence recording through iconography only.
```
